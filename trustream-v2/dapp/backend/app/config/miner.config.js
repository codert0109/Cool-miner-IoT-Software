module.exports = {
    MINEDATA_TIME_OUT : 10 * 60,         // We detect miner is not working 
                                        // if the last uploading data time is more than MINEDATA_TIME_OUT seconds before.
    UPLOAD_TIME_INTERVAL : 2
}